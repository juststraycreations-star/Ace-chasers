/**
 * Email verification banner — intentionally disabled.
 *
 * To re-enable in the future, restore the previous version from git that
 * shows the yellow nudge bar with a "Resend email" button when the
 * profile has `emailVerified === false`.
 */
export default function EmailVerificationBanner() {
  return null;
}
