# ── Android build artefacts ──────────────────────────────────────
# Local secrets — NEVER commit the real values.
keystore.properties
*.jks
*.keystore

# Gradle + Android Studio caches
.gradle/
build/
local.properties
captures/
.idea/
*.iml

# Capacitor-generated native bits that get regenerated by cap sync
app/src/main/assets/public/
app/src/main/assets/capacitor.config.json
app/src/main/assets/capacitor.plugins.json
